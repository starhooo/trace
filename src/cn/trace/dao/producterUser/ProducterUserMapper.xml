<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="cn.trace.dao.producterUser.ProducterUserMapper">
	<!-- getLoginWorks -->
	<select id="selectProducterUser"  resultType="producterUser"  parameterType="producterUser">
		select * from producteruser  where usernum = #{usernum} and password =#{password}
	</select>

</mapper>
